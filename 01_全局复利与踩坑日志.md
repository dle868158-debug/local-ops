# 01_全局复利与踩坑日志

> 本项目「本地服务指挥台」（总控台）的稳定经验、踩坑记录与可复用方案。
> 新发现的经验持续追加，不只留在聊天记录里。

---

## 2026-08-22 会话：网址卡片白屏 + 重新打包 + 环境变量坑

### 1. 网址卡片"点开没反应"的真身：后端 API 返回 Vite 空壳 HTML

- **现象**：在启动台添加指向 `http://127.0.0.1:5409/` 的网址卡片，点「打开」浏览器弹出了但**页面纯白**，用户描述为"点开没反应"。
- **根因**：5409 是**后端 API 服务**（`app.py`，自媒体多平台发布后端），不是网页。它的 `/` 根路径只返回一段 Vite 开发模板空壳：

  ```html
  <div id="app"></div>
  <script type="module" src="/src/main.js"></script>
  ```

  而 `/src/main.js`、`/vite.svg` 在 5409 上全部 **404** → JS 加载失败 → 页面永远白屏。
- **正确网页**：前端 dev server 在 **5173**（千帆云递 · QianFan Sync UI，Vue/Vite），用户登录小红书/抖音/B站用的就是它。
- **教训**：
  - 网址卡片要指向**前端 UI 端口**，不要指向后端 API 端口；后端根路径 ≠ 网页。
  - 判定方法：用 `Invoke-WebRequest` 抓根路径 HTML，看引用的 JS/CSS 资源在该端口是否 200。

### 2. `127.0.0.1` 与 `localhost` 的 IPv6 坑

- **现象**：`http://127.0.0.1:5173/` 连不上，但 Chrome 历史里 `http://localhost:5173/` 能打开。
- **根因**：Vite dev server 只监听了 **IPv6 `::1`**（`Get-NetTCPConnection` 显示 `LocalAddress = ::1`），没有监听 IPv4 `127.0.0.1`。`localhost` 会先解析 IPv6 `::1`，所以能通。
- **教训**：排查端口连通性时，`127.0.0.1` 连不上 ≠ 服务没起，先看监听地址是 `::1` 还是 `0.0.0.0`/`127.0.0.1`；网址卡片 URL 写成 `http://localhost:5173/` 更稳。

### 3. PyInstaller 打包版 exe 无法覆盖：Windows 允许"重命名"正在运行的 exe

- **现象**：重新打包时 `PyInstaller` 报 `PermissionError: [WinError 5] 拒绝访问: dist\总控台.exe`，因为旧实例（含 `--tool-anchor` 服务锚点进程）正在运行、锁着 exe。
- **解决**：不要 `taskkill`（会连带停掉托管服务，比如 3080 的 dsh web），而是先**重命名**旧 exe 腾出路径：

  ```powershell
  Rename-Item -LiteralPath 'dist\总控台.exe' -NewName '总控台.exe.old'
  ```

  再重新打包写回 `dist\总控台.exe`。运行中的进程继续从 `.old` 运行不受影响；等其退出后可删 `.old`。
- **教训**：Windows 上"正在执行的 exe 可以重命名、不能删除/覆盖"——这是替换运行中程序的标准手法（Chrome 自动更新同款原理）。

### 4. `_PYI_*` 环境变量坑：PyInstaller 6.x 安全校验 `Security validation failure: parent process has different executable!`

- **现象**：新打包的 exe 从 PowerShell/ShellExecute/explorer 启动都弹错误框：

  ```
  Security validation failure: parent process has different executable!
  ```

  bootloader 卡住（无 `_MEI` 解压目录、无日志、CPU 0），但用户双击却正常（旧 exe 一直正常）。
- **根因**：PyInstaller ≥ 6.x（含 GHSA-9fxf-4qw3-ghmr 修复）在检测到继承的 PyInstaller 运行环境时，会校验**父进程 exe 是否与自身相同**。旧总控台实例启动时把三个环境变量写进了进程树：

  ```
  _PYI_ARCHIVE_FILE=C:\...\dist\总控台.exe
  _PYI_APPLICATION_HOME_DIR=%TEMP%\_MEI000010342
  _PYI_PARENT_PROCESS_LEVEL=1
  ```

  这些变量沿 旧总控台 → 锚点进程(`--tool-anchor`) → dsh web → 我的终端(pwsh) 一路继承。我的 pwsh 启动新 exe 时，新 exe 发现 `_PYI_ARCHIVE_FILE` 恰好等于自己路径 → 判定"继承环境" → 校验父进程（pwsh.exe ≠ 总控台.exe）→ 拒绝启动。
- **解决**：启动前清除变量：

  ```powershell
  Remove-Item env:_PYI_ARCHIVE_FILE, env:_PYI_APPLICATION_HOME_DIR, env:_PYI_PARENT_PROCESS_LEVEL, env:_PYI_SPLASH_IPC -ErrorAction SilentlyContinue
  ```

  清除后新 exe 2~3 秒正常启动。
- **教训**：
  - **用户双击启动不受影响**（explorer 环境干净）——从终端/脚本验证 PyInstaller 打包产物时若遇此错，先查 `_PYI_*` 环境变量。
  - 排查顺序：错误框文本（UI Automation 读 `#32770` 对话框子控件）→ `_PYI_*` 环境变量 → 干净环境复测。

### 5. 总控台 PUT API 部分更新 link 卡必须带 `kind:"link"`

- **现象**：`PUT /api/apps/{id}` 只传 `{name, url}` 更新网址卡片返回 `400 url 只适用于网址卡片（kind=link）`。
- **根因**：`server.py` 更新校验里，`kind` 不在请求体时 `fields.get("kind")` 为 `None`，走不到 link 分支（见 `server.py` 约 3936-3947 行），于是带 `url` 就被误拒。前端编辑面板会带完整字段所以正常，纯 API 部分更新会踩。
- **解决**：请求体显式带 `"kind":"link"`；另外 **PowerShell 5.1 的 `Invoke-WebRequest` 发 JSON 中文会变 `?`**（编码问题），改用 Python `urllib` + `json.dumps`（UTF-8）最稳。

### 6. 判断浏览器是否真的打开了网址：读 Chrome 历史

- 排查"点开有没有反应"时，除了看接口返回，可以直接读 Chrome 历史确认 URL 是否真的被访问过：

  ```python
  import sqlite3, datetime
  db = sqlite3.connect(r"C:\Users\...\AppData\Local\Google\Chrome\User Data\Default\History")  # 需先复制，文件被锁
  # last_visit_time 是 1601-01-01 起的微秒数；UTC +8 = 北京时间
  ```

- 本次靠它确认：接口 `open` 两次都成功且 Chrome 历史出现 3080/5409 记录，而用户手动点击时段无记录 → 定位到"点了但白屏"而非"点了没弹"。

---

## 可复用清单

| 场景 | 一句话方案 |
|---|---|
| 网址卡白屏 | 先抓根路径 HTML 看是否空壳；确认指向前端端口而非 API 端口 |
| 端口连不上 | `Get-NetTCPConnection` 看监听地址是 `::1`（用 localhost）还是 `0.0.0.0`/`127.0.0.1` |
| 打包时 exe 被锁 | 重命名旧 exe 腾路径，别杀进程（会连累托管服务） |
| 新 exe 启动弹 Security validation | 清 `_PYI_*` 环境变量后再启动；双击不受影响 |
| API 部分更新 link 卡 | 请求体带 `kind:"link"`；中文用 Python urllib 发 |
| 验证浏览器是否打开 | 复制 Chrome History 到临时文件，用 sqlite3 查最近 url |
